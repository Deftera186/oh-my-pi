# omp-collab

`omp-collab` owns OMP's versioned, bounded collaboration relay substrate: room cryptography, revision-3 JSON framing with the browser-compatible four-byte relay envelope, deterministic replication reduction, and reconnect transport state. It deliberately has no application or UI dependencies and implements the same `/r/<room>`, AES-GCM, envelope, and frame grammar as `collab-web`.
